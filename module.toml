name = "blvm-ui"
version = "0.1.0"
description = "Optional localhost (later web) UI for blvm-node"
author = "Bitcoin Commons Contributors"
entry_point = "blvm-ui"

capabilities = [
    "read_blockchain",
    "read_chain_state",
    "subscribe_events",
]
